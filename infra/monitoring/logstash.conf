input {
  beats {
    port => 5044
  }
}

filter {
  if [fields][service] == "revenue-api" {
    mutate { add_tag => ["revenue"] }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "revenue-%{+YYYY.MM.dd}"
  }
}
