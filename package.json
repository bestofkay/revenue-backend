{
  "name": "revenue-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Government Enterprise Revenue Collection Platform",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "typecheck": "turbo run typecheck",
    "db:generate": "pnpm --filter @revenue/database generate",
    "db:migrate": "pnpm --filter @revenue/database migrate:dev",
    "db:migrate:deploy": "pnpm --filter @revenue/database migrate:deploy",
    "db:seed": "pnpm --filter @revenue/database seed",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
